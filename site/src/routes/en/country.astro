---
import CountryPage from "../../components/country-page.astro";
import { countries } from "../../lib/data";

export function getStaticPaths() {
  return countries.map((country) => ({ params: { id: country.id } }));
}

const { id } = Astro.params;
---

<CountryPage id={id!} lang="en" />
